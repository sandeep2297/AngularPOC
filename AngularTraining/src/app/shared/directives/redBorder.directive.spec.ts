/* tslint:disable:no-unused-variable */

import { ElementRef, Renderer2 } from '@angular/core';
import { TestBed, async, inject } from '@angular/core/testing';
import { RedBorderDirective } from './redBorder.directive';


describe('RedBorderDirective', () => {
  // it('should create an instance', inject( [ElementRef, Renderer2], (eleRef: ElementRef, renderer: Renderer2) => {
  //   const directive = new RedBorderDirective(eleRef, renderer);
  //   expect(directive).toBeTruthy();
  // }));
});
